{
  "name": "world-needs-heroes",
  "private": true,
  "version": "1.0.0",
  "description": "Check the latest Overwatch play of the game and vote for the best one on World Needs Heroes",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-clean-css": "^2.0.10",
    "gulp-concat": "^2.6.0",
    "gulp-sourcemaps": "^1.6.0",
    "gulp-uglify": "^1.5.4",
    "http-server": "^0.9.0",
    "shelljs": "^0.6.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app/dist",
    "gulp watch": "gulp watch",
    "firebase deploy": "firebase deploy"
  }
}
